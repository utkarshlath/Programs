#include<bits/stdc++.h>
using namespace std;

typedef long long int ll;

int main()
{
    ll n,m,total_max=0;
    cin>>n>>m;
    vector<ll> v(n);
    for(ll i=0;i<n;i++)
        cin>>v[i];
    
    cout<<total_max;
    return 0;
}